receivers:
  otlp:
    protocols:
      grpc:
      http:
exporters:
  jaeger:
    endpoint: "jaeger:14250"
    tls:
      insecure: true
      insecure_skip_verify: true
  otlp:
    endpoint: tempo:4317
    tls:
      insecure: true
    headers:
      x-scope-orgid: "Organization 1"

processors:
  batch:

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [jaeger, otlp]
      processors: [batch]
  telemetry:
    logs:
      level: "debug"